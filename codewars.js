// Given a string of digits, you should replace any digit below 5 with '0' and any digit 5 and above with '1'. Return the resulting string.

// Note: input will never be an empty string

// x -> string

// Step 1 -> Convert string into array
// Step 2 -> Loop through the array based on condition
// Step 3 -> Convert Array back into string and return the result.

// function fakeBin(x) {
//   return x
//     .split("")
//     .map((n) => (n < 5 ? 0 : 1))
//     .join("");
// }

// console.log(fakeBin("234567890"));

// // Calculate Sum of Arrays

// let arr = [12, 34, 56, 34, 67, 89];

// function sumOfArray(arr) {
//   let sumNum = 0;
//   for (let i = 0; i < arr.length; i++) {
//     sumNum = sumNum + arr[i];
//   }
// //   console.log(sumNum);
//   return sumNum;
// }
// sumOfArray(arr);


// const words = ['spray', 'elite', 'exuberant', 'destruction', 'present'];

// // Filter this array based on 5 letters

// const result = words.filter(word=> word.length === 5)
// console.log(result);

// 1 -> 1 odd no
// 2-> 2 Consecutive Odd nos -> Sum
// 3- > 3 Consecutive Odd nos -> SUm
// 4 -> 4 Consecutive Odd nos -> Sum


function rowSumOddNumbers(n) {
	// TODO
  
//   for(let i =0; i<n-1; i++) {
    
//   }
let n = 2;
const arrOfOddNos = ['2 items']
for (const i of arrOfOddNos) {
    // sum logic
}
}
arrOfOddNos